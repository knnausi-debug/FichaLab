/**
 * URL base del backend (sin /api al final).
 *
 * Local: déjalo vacío → usa /api en el mismo servidor (npm start).
 * Producción (Netlify): API en Railway.
 */
window.FICHALAB_API_BASE = "https://fichalab-production.up.railway.app";

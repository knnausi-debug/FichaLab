/**
 * URL base del backend (sin /api al final).
 *
 * Local: déjalo vacío → usa /api en el mismo servidor (npm start).
 * Netlify: pon la URL de tu API en Render/Railway, ej:
 *   window.FICHALAB_API_BASE = "https://fichalab-api.onrender.com";
 */
window.FICHALAB_API_BASE = window.FICHALAB_API_BASE || "";
